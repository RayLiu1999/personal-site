# 此檔案包含敏感資訊，請勿提交到版本控制
# 使用方式：
#   1. 複製此檔案為 secret.yaml
#   2. 填入實際的密鑰
#   3. kubectl apply -f k8s/secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: personal-site-secret
  namespace: default
type: Opaque
stringData:
  # LINE Bot credentials
  LINE_CHANNEL_ACCESS_TOKEN: ""  # 你的 LINE channel access token
  LINE_CHANNEL_SECRET: ""         # 你的 LINE channel secret
  LINE_USER_ID: ""                # 你的 LINE user ID
  
  # reCAPTCHA secret key
  RECAPTCHA_SECRET_KEY: ""        # 你的 reCAPTCHA secret key
